# Architecture Docs

Use this directory for project-wide architecture documents when they are useful. Examples include global constraints, interface conventions, language or framework standards, cross-module architecture notes, or decision records.

The default harness does not require a fixed document set, source-tree mirroring, or proposal/design documents here. Repo-local project rules decide which files belong in this directory and how they are validated.
