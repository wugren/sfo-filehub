schema_version: 1

# 用户 2026-08-27 回复“确认，按standard任务完成就好”：确认提案范围并显式
# 选择 standard，接受已展示的配置兼容、部署与依赖风险。
workflow_tier: standard

version: v0.1
packet_module: filehub
task_name: 055-server-yaml-config
stage: general
mode: manual
auto_pipeline_start_stage:

proposal: proposal.md
completion_report: completion-report.md
change_record: docs/changes/055-server-yaml-config.md
design: design.md
testing: testing.md
testplan: testplan.yaml
acceptance_report: acceptance-report.md
pipeline_plan: pipeline/plan.md
risk_profile: risk-profile.yaml
lifecycle_state: lifecycle.json
changed_paths_file: .harness/evidence/v0.1/stage-scope/055-server-yaml-config.paths
baseline_manifest: .harness/baselines/v0.1/055-server-yaml-config-delivery/manifest.json

changes:
  - id: fh-server-yaml-loader
    target_module: filehub
    scope_paths: [server/src/main.rs, server/Cargo.toml, Cargo.lock]
    changed_paths_file:
  - id: fh-server-yaml-examples
    target_module: filehub
    scope_paths: [server/config.example.json, server/config.example.yaml, filehub-server.json, filehub-server.yaml, server/tests/unit/config.rs]
    changed_paths_file:
  - id: fh-server-yaml-deployment
    target_module: filehub
    scope_paths: [docker/entrypoint.sh, docker/README.md, README.md, docs/modules/filehub.md]
    changed_paths_file:
